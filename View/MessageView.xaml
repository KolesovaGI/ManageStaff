<Window x:Class="ManageStaffDBApp.View.MessageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ManageStaffDBApp.View"
        mc:Ignorable="d"
        Title="ВНИМАНИЕ" Height="100" Width="200" ResizeMode="NoResize" Icon="/View/logo.png" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="MessageText" Grid.Row="0"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   TextWrapping="Wrap"/>
        <Button Grid.Row="1" Content="OK" Click="Button_Click"
                FontWeight="Bold" FontSize="15" Foreground="#B39EB5" 
                Background="{x:Null}" BorderBrush="{x:Null}"/>
    </Grid>
</Window>
